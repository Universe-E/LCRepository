import java.util.Arrays;
import java.util.TreeSet;

class Solution {
    public int numTeams(int[] rating) {
        int n = rating.length;
        int[] arr = Arrays.copyOf(rating,n);
        TreeSet<Integer> set1 = new TreeSet<>();
        set1.subSet()
    }
}