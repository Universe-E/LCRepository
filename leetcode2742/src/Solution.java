import java.util.Arrays;

class Solution {
    public int paintWalls(int[] cost, int[] time) {
        int n = cost.length;
        Integer[] idx = new Integer[n];
        for (int i = 0; i < n; i++) {
            idx[i] = i;
        }
        Arrays.sort();
    }
}